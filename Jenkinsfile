pipeline {
     agent {label 'master'}
     stages {
        stage ('Initialize') {
            steps {
                sh "mvn clean install"
            }
        }
        stage('Build') { 
            steps {
               
            }
        }
    }
}
